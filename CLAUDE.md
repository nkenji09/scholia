# この repo で作業するときの約束

## 規律をどこに置くか

- **振る舞いの決定と、その理由**は `.scholia`（正本）に置く。実装より先に正本、が原則。
- **書き方の規律**——人や AI が文章やコードを書くときの形の指示——は、次のいずれかに置く。
  - 機械で判定できるなら **lint 規則か保存時ゲート**（`internal/lint/`・`config.idPolicy`）
  - scholia レコードの書き方なら **配布スキル**（`agents/skills/`・`_scholia-shared/references/`）
  - この repo の中だけで守る書き方なら **この CLAUDE.md**

書き方の規律を正本に書いても、違反は誰にも止められない。
根拠は decision `01KXS68HCNQ0H9QKNYFQ869J19`——「明文化しても遡及機構が無ければ挙動は変わらない」ことは、
命名規約が明文化後に作られたレコードでも守られなかったという実測で確かめられている。

---

## 配線ガード（テスト）の書き方

「共通の配線が外れていないこと」を守るテストを**配線ガード**と呼ぶ。
このガードは自称より弱くなりやすく、この repo では同じ型の抜けが繰り返し起きている。

### 1. 判断は純関数へ切り出し、入力と出力の対で検査する

「出すか出さないか」「どう数えるか」といった判断を、画面から切り離して呼べる形にする。
そのうえで**入力に対する答え**を検査する。画面を起こしてソースを覗く検査より強い。

この形へ直した箇所は、以後、同じ型で再発していない。有効性は実測済み。

### 2. ソース文字列の照合をガードと呼ばない

「この呼び出しが書かれている」「この条件式が書かれている」だけを見る検査は、
**同じ意味を別の綴りで書かれれば捕まらない**。これは検査の書き方を工夫しても原理的に消えない。

だから、捕まえられない綴りを1つずつ列挙してはいけない。**この原理を1つ書いて、射程を名乗る。**

> 2026-07-28 の差し戻しでは、最終ラウンドだけで新しい抜け道が5件見つかった。
> 5件はすべてこの1つの原理の別インスタンスだった。列挙で追う限り、差し戻しは終わらない。

### 3. 構造を見るなら「どのゲートの内側にもいないこと」まで一般化する

ある1つのゲートの内側にいないことだけを見る実装は、**別のゲートで包む変異**を通す。
実例: marker を `indexOf` で探す形は、**先頭のゲートしか取れない**。

### 4. ガードを足したら、自分で変異を入れて red を実見する

レビュアにも変異試験を要求する。「red を実見した」という申告は、
**ガードの射程が足りていなくても真になりうる**——実際にそうなった。

### 5. 新しく作った面には、ガードを置き忘れる

面を足したら、その面にも同じガードが効いているかを確かめる。
ガードを主題にした作業でさえ、新設した面のガード漏れが最後まで残った。

### 6. 「歯止めを置いた」と書くときは、何に対して落ちるのかまで書く

ガードが称する範囲と、実際に落ちる範囲が違えば、
**正本が実装を誤って記述している**のと同じ状態になり、`lint --ci` は緑のまま何も知らせない。

---

## viewer の配線

新しいページ・新しい面を足すときは、既にある共有機構を配線する。**面ごとに独自機構を作らない。**

- 左ドロワーの横幅リサイズ（共有 Resizer ＋ 永続）
- 状態保持（スクロール位置・絞り込み・折りたたみ）
- 別レコードへ移動する行は、`onClick` のボタンではなく本物の `<a href>` で描く

いずれも、配線漏れが実際に起きた箇所である。

---

## 検証

Go を触ったら次を通す（linked worktree では `go build` が buildvcs スタンプで落ちうるので `-buildvcs=false` で判断してよい）。

```sh
go build ./... && go vet ./... && go test ./... && gofmt -l . && scholia lint --ci
```

`web/dist` は、`web/` のソースを変えたときだけ再生成する。
